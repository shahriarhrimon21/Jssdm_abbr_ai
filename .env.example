# Copy this file to .env for local `netlify dev` use (never commit .env).
# In production, set this in Netlify: Site settings -> Environment variables.
# The key is read ONLY by the server-side function in netlify/functions/ai.ts
# and is never sent to, or bundled into, the browser code.

GEMINI_API_KEY=
